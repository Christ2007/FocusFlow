# Frontend (Vite) environment variables
# Copy this file to .env.local and adjust values as needed.
# Vite only exposes variables prefixed with VITE_.

# Base URL for your backend API. Include the /api suffix.
# Examples:
# Localhost:
# VITE_API_BASE_URL=http://localhost:5000/api
# LAN (replace with your machine's IP):
VITE_API_BASE_URL=http://10.0.0.34:5000/api

# If VITE_API_BASE_URL is not set, the app falls back to:
#   http://<current-hostname>:5000/api
